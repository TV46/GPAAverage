name: Build Executable

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Inject subdomain
        env:
          SCHOOL_SUBDOMAIN: ${{ secrets.SCHOOL_SUBDOMAIN }}
        run: |
          (Get-Content main.py) -replace 'SCHOOL_SUBDOMAIN_PLACEHOLDER', $env:SCHOOL_SUBDOMAIN | Set-Content main.py
        shell: powershell

      - name: Build one-file executable
        run: >
          pyinstaller
          --onefile
          --name GPAAverage
          --clean
          main.py

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: GPAAverage
          path: dist/GPAAverage.exe
          retention-days: 30
